{
  "mcpServers": {
    "mailersend": {
      "command": "node",
      "args": [
        "./scripts/mailersend-mcp.mjs"
      ],
      "env": {
        "MAILERSEND_API_TOKEN": "${MAILERSEND_API_TOKEN}"
      }
    }
  }
}
